# 🎨 Visual Guide to JECH

**For developers who learn better with pictures!**

This guide uses diagrams, flowcharts, and visual representations to explain how JECH works.

---

## 🌊 The Complete Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    YOUR JECH CODE                           │
│                  say("Hello, World!");                      │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    STAGE 1: TOKENIZER                       │
│                    (Breaking into pieces)                   │
│                                                             │
│  Input:  say("Hello, World!");                             │
│                                                             │
│  Output: ┌─────┐ ┌───┐ ┌───────────────┐ ┌───┐ ┌───┐     │
│          │ say │ │ ( │ │ "Hello, World!"│ │ ) │ │ ; │     │
│          └─────┘ └───┘ └───────────────┘ └───┘ └───┘     │
│          KEYWORD LPAREN    STRING        RPAREN SEMI       │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    STAGE 2: PARSER                          │
│                  (Understanding structure)                  │
│                                                             │
│  Input:  [say] [(] ["Hello, World!"] [)] [;]              │
│                                                             │
│  Checks: ✓ say followed by (                              │
│          ✓ ( contains a value                             │
│          ✓ ) closes the parentheses                       │
│          ✓ ; ends the statement                           │
│                                                             │
│  Output: SAY_STATEMENT                                     │
│          └── value: "Hello, World!"                        │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    STAGE 3: AST                             │
│                  (Building a tree)                          │
│                                                             │
│         SAY_NODE                                            │
│            │                                                │
│            ├── type: JECH_AST_SAY                          │
│            ├── value: "Hello, World!"                      │
│            └── token_type: TOKEN_STRING                    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                  STAGE 4: BYTECODE                          │
│              (Simplified instructions)                      │
│                                                             │
│  Input:  SAY_NODE { value: "Hello, World!" }              │
│                                                             │
│  Output: [0] OP_SAY                                        │
│              operand: "Hello, World!"                      │
│              token_type: TOKEN_STRING                      │
│          [1] OP_END                                        │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    STAGE 5: VM                              │
│                  (Actually running)                         │
│                                                             │
│  Step 0: Read instruction OP_SAY                           │
│          Get operand: "Hello, World!"                      │
│          Execute: printf("Hello, World!\n")                │
│                                                             │
│  Step 1: Read instruction OP_END                           │
│          Stop execution                                    │
│                                                             │
│  Terminal Output: Hello, World!                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔤 Tokenizer in Detail

### The Process

```
Input String: "keep x = 10;"
       │
       ▼
┌──────────────────────────────────────┐
│  Character-by-character scanning     │
│                                      │
│  k → Part of word                   │
│  e → Part of word                   │
│  e → Part of word                   │
│  p → Part of word                   │
│  [space] → Word complete!           │
│                                      │
│  Word: "keep"                       │
│  Check: Is it a keyword?            │
│  Yes! → TOKEN_KEEP                  │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Continue scanning...                │
│                                      │
│  x → Part of word                   │
│  [space] → Word complete!           │
│                                      │
│  Word: "x"                          │
│  Check: Is it a keyword?            │
│  No → TOKEN_IDENTIFIER              │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Continue scanning...                │
│                                      │
│  = → Single character token         │
│  → TOKEN_EQUAL                      │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Continue scanning...                │
│                                      │
│  1 → Start of number                │
│  0 → Part of number                 │
│  ; → Number complete!               │
│                                      │
│  Number: "10"                       │
│  → TOKEN_NUMBER                     │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Last token                          │
│                                      │
│  ; → TOKEN_SEMICOLON                │
└──────────────┬───────────────────────┘
               │
               ▼
        Final Token List:
        ┌──────────────┐
        │ TOKEN_KEEP   │
        │ TOKEN_IDENT  │
        │ TOKEN_EQUAL  │
        │ TOKEN_NUMBER │
        │ TOKEN_SEMI   │
        │ TOKEN_EOF    │
        └──────────────┘
```

---

## 🧱 Parser Pattern Matching

### Example: `say("Hello");`

```
Token Stream: [say] [(] ["Hello"] [)] [;]
                │
                ▼
        ┌───────────────┐
        │ Is it "say"?  │
        └───────┬───────┘
                │ YES
                ▼
        ┌───────────────┐
        │ Next is "("?  │
        └───────┬───────┘
                │ YES
                ▼
        ┌───────────────┐
        │ Next is value?│
        └───────┬───────┘
                │ YES (string)
                ▼
        ┌───────────────┐
        │ Next is ")"?  │
        └───────┬───────┘
                │ YES
                ▼
        ┌───────────────┐
        │ Next is ";"?  │
        └───────┬───────┘
                │ YES
                ▼
        ┌───────────────┐
        │ Create SAY    │
        │ AST node      │
        └───────────────┘
```

### Example: Error Detection

```
Token Stream: [say] ["Hello"] [;]
                │
                ▼
        ┌───────────────┐
        │ Is it "say"?  │
        └───────┬───────┘
                │ YES
                ▼
        ┌───────────────┐
        │ Next is "("?  │
        └───────┬───────┘
                │ NO! It's a string
                ▼
        ┌───────────────┐
        │ ❌ ERROR!     │
        │ Expected '('  │
        │ after 'say'   │
        └───────────────┘
```

---

## 🌳 AST Structure Examples

### Simple Statement

```jc
say("Hello");
```

```
        SAY
         │
         └── "Hello"
```

### Variable Declaration

```jc
keep x = 10;
```

```
        KEEP
         │
         ├── name: "x"
         └── value: 10
```

### Conditional

```jc
when (x > 5) {
    say("Big");
}
```

```
           WHEN
            │
            ├── condition
            │      │
            │      └── BIN_OP (>)
            │           │
            │           ├── left: x
            │           └── right: 5
            │
            └── body
                   │
                   └── SAY
                        │
                        └── "Big"
```

### Array

```jc
keep arr = [1, 2, 3];
```

```
           KEEP
            │
            ├── name: "arr"
            │
            └── ARRAY_LITERAL
                    │
                    ├── [0]: 1
                    ├── [1]: 2
                    └── [2]: 3
```

---

## 🔧 Bytecode Instructions

### Visual Representation

```
Source:  keep x = 10;
         say(x);

Bytecode:
┌────┬──────────┬──────────┬───────┐
│ PC │ OpCode   │ Name     │ Value │
├────┼──────────┼──────────┼───────┤
│ 0  │ OP_KEEP  │ "x"      │ "10"  │
│ 1  │ OP_SAY   │ "x"      │       │
│ 2  │ OP_END   │          │       │
└────┴──────────┴──────────┴───────┘

PC = Program Counter (which instruction we're on)
```

### Execution Flow

```
┌─────────────────────────────────────┐
│ VM starts at PC = 0                 │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Read instruction at PC              │
│ OP_KEEP name="x" value="10"        │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Execute OP_KEEP                     │
│ • Create variable "x"               │
│ • Store value "10"                  │
│ • Variables: { x: 10 }              │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Increment PC (now PC = 1)           │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Read instruction at PC              │
│ OP_SAY operand="x"                 │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Execute OP_SAY                      │
│ • Look up "x" in variables          │
│ • Found: "10"                       │
│ • Print: "10"                       │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Increment PC (now PC = 2)           │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Read instruction at PC              │
│ OP_END                              │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Stop execution                      │
│ Output: 10                          │
└─────────────────────────────────────┘
```

---

## 🎯 Memory Management

### Variable Storage

```
Source: keep x = 10;
        keep name = "João";
        keep active = true;

VM Memory:
┌─────────────────────────────────┐
│      VARIABLE TABLE             │
├──────────┬──────────────────────┤
│ Name     │ Value                │
├──────────┼──────────────────────┤
│ "x"      │ "10"                 │
│ "name"   │ "João"               │
│ "active" │ "true"               │
└──────────┴──────────────────────┘
```

### Array Storage

```
Source: keep arr = [1, 2, 3];

VM Memory:
┌─────────────────────────────────┐
│      VARIABLE TABLE             │
├──────────┬──────────────────────┤
│ Name     │ Value                │
├──────────┼──────────────────────┤
│ (empty - arrays stored separately)│
└─────────────────────────────────┘

┌─────────────────────────────────┐
│       ARRAY TABLE               │
├──────────┬──────────────────────┤
│ Name     │ Elements             │
├──────────┼──────────────────────┤
│ "arr"    │ [0]: "1"             │
│          │ [1]: "2"             │
│          │ [2]: "3"             │
│          │ size: 3              │
└──────────┴──────────────────────┘
```

---

## 🔄 Comparison with Python

### Python's Pipeline

```
Python Code: print("Hello")
      │
      ▼
┌─────────────┐
│ Tokenizer   │ → [NAME: print] [OP: (] [STRING: "Hello"] [OP: )]
└──────┬──────┘
       ▼
┌─────────────┐
│ Parser      │ → Call expression
└──────┬──────┘
       ▼
┌─────────────┐
│ AST         │ → Call(func=Name('print'), args=[Str('Hello')])
└──────┬──────┘
       ▼
┌─────────────┐
│ Compiler    │ → LOAD_NAME print
└──────┬──────┘    LOAD_CONST "Hello"
       ▼           CALL_FUNCTION 1
┌─────────────┐
│ CPython VM  │ → Execute bytecode
└──────┬──────┘
       ▼
    Output: Hello
```

### JECH's Pipeline (Same Concept!)

```
JECH Code: say("Hello")
      │
      ▼
┌─────────────┐
│ Tokenizer   │ → [SAY] [(] ["Hello"] [)] [;]
└──────┬──────┘
       ▼
┌─────────────┐
│ Parser      │ → Say statement
└──────┬──────┘
       ▼
┌─────────────┐
│ AST         │ → SAY_NODE { value: "Hello" }
└──────┬──────┘
       ▼
┌─────────────┐
│ Compiler    │ → OP_SAY "Hello"
└──────┬──────┘
       ▼
┌─────────────┐
│ JECH VM     │ → Execute bytecode
└──────┬──────┘
       ▼
    Output: Hello
```

**See? Identical process!**

---

## 🎨 Color-Coded Example

```
Source Code:
keep x = 10;

Tokenization:
🔵 keep  ← Keyword
🟢 x     ← Identifier
🟡 =     ← Operator
🟠 10    ← Number
🔴 ;     ← Punctuation

Parsing:
📦 KEEP_STATEMENT
   ├─ 🟢 name: "x"
   └─ 🟠 value: "10"

Bytecode:
⚙️ OP_KEEP name="x" value="10"

Execution:
💾 Store in memory: x → 10
```

---

## 📊 Decision Trees

### Token Type Decision

```
Character encountered
        │
        ├─ Letter? → Read word
        │              │
        │              ├─ Keyword? → TOKEN_KEYWORD
        │              ├─ "true"/"false"? → TOKEN_BOOL
        │              └─ Other? → TOKEN_IDENTIFIER
        │
        ├─ Digit? → Read number → TOKEN_NUMBER
        │
        ├─ Quote? → Read string → TOKEN_STRING
        │
        └─ Symbol? → Check which
                      │
                      ├─ ( → TOKEN_LPAREN
                      ├─ ) → TOKEN_RPAREN
                      ├─ ; → TOKEN_SEMICOLON
                      ├─ = → TOKEN_EQUAL
                      └─ etc.
```

---

## 🎓 Learning Path Visualization

```
START
  │
  ▼
┌────────────────┐
│ Understand     │ ← You are here!
│ Concepts       │   (This guide)
└───────┬────────┘
        │
        ▼
┌────────────────┐
│ See Examples   │
│ in Action      │
└───────┬────────┘
        │
        ▼
┌────────────────┐
│ Read Component │
│ Documentation  │
└───────┬────────┘
        │
        ▼
┌────────────────┐
│ Examine        │
│ Source Code    │
└───────┬────────┘
        │
        ▼
┌────────────────┐
│ Contribute!    │
└────────────────┘
```

---

## 🔍 Debugging Visualization

### When Something Goes Wrong

```
Error: "Expected ')' after value"

Source: say("Hello";
            ↑      ↑
            │      └─ Found ';' instead of ')'
            └─ Started here

Token Stream:
[say] [(] ["Hello"] [;]
                     ↑
                     Problem here!

Expected:
[say] [(] ["Hello"] [)] [;]
                     ↑
                     Should be ')'
```

---

## 💡 Quick Reference

### Pipeline Stages

| Stage | Input | Output | Analogy |
|-------|-------|--------|---------|
| **Tokenizer** | Text | Tokens | Reading words |
| **Parser** | Tokens | AST | Grammar check |
| **AST** | Structure | Tree | Family tree |
| **Bytecode** | Tree | Instructions | Recipe steps |
| **VM** | Instructions | Results | Following recipe |

### Common Patterns

```
Variable:     keep NAME = VALUE;
Array:        keep NAME = [VALUE, VALUE, ...];
Print:        say(VALUE);
Array access: say(ARRAY[INDEX]);
Conditional:  when (CONDITION) { BODY }
```

---

**Remember:** Every diagram here represents something that happens in Python, JavaScript, and PHP too. You're learning universal concepts!

**Next:** Try the [Interactive Tutorial](FOR_BEGINNERS.md) to see these concepts in action!
