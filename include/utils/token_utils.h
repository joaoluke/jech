#ifndef TOKEN_UTILS_H
#define TOKEN_UTILS_H

#include "core/tokenizer.h"

/**
 * Converts a JechTokenType enum value to a human-readable string.
 */
const char *token_type_to_str(JechTokenType type);

#endif
