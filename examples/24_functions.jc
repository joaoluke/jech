// Funções que executam código
do saudacao(nome) {
    say("Ola ");
    say(nome);
}

// Chamando a função
saudacao("Visitante");

// Função com retorno
do soma(a, b) {
    return a + b;
}

keep resultado = soma(3, 5);
say(resultado);
