// Funções que executam código
keep nome = "Visitante";

do saudacao() {
    say("Ola ");
    say(nome);
}

// Chamando a função
saudacao();

// Função com retorno
do soma(a, b) {
    return a + b;
}

keep resultado = soma(1, 2);
say(resultado);
